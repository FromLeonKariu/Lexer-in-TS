# Todo
### ADD lookahead to implement maximal munch
### Add string literals processing and comment handling processing
### read more on optimization of code to see if i can increase its performance
### Figure out a way to make the tokinizer decide the mode/state according of the first char it encounters and only keep collecting the same kind of char i.e nums, or alphanumericals. || if it hits something else it stops
### continue dev to make it a parser -> then later a small compiler for my language popo
